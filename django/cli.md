# Create new
`> django-admin startproject mysite`

# Migrate (init DB tables)
`> python3 manage.py migrate`

# Run dev server
`> python3 manage.py runserver`
`> python3 manage.py runserver 127.0.0.1:8000 \--settings=mysite.settings`
